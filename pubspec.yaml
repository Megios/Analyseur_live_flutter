name: flutter_live_appplication_analyse
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.1 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  # section ou on indique à flutter ou se trouve notre polices de caractères
  fonts:
    - family: Pacifico
      fonts:
        - asset: fonts/Pacifico.ttf
  # section ou on indique a flutter ou trouver nos images
  # assets:
  #   - images/
